import type { Metadata } from "next";
import { DM_Sans } from "next/font/google";
import "./globals.css";

const dmSans = DM_Sans({
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700"],
  variable: "--font-dm-sans",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Vizolane Technologies LLP | Smart City Infrastructure",
  description:
    "Vizolane creates AI-powered real-time systems for smarter cities — intelligent traffic signals, tolling, and urban transportation.",
  openGraph: {
    title: "Vizolane Technologies LLP",
    description: "AI-Powered Smart City Infrastructure & Digital Innovation",
    type: "website",
    locale: "en_IN",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={dmSans.variable}>
      <body>{children}</body>
    </html>
  );
}
